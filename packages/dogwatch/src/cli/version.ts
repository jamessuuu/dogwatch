/** SPEC §3: `watchVersion` / `checkPackVersion` stamped into every record. */
export const DOGWATCH_VERSION = "0.1.0-alpha.0";
/** Bumped independently when the rule catalog (src/checks) changes shape,
 * not on every code release. */
export const CHECK_PACK_VERSION = "1";
